# =============================================================================
# SyllogismTechnologyAfrica — environment template (COPY to .env and fill in).
# SECURITY: NEVER commit the real .env. Keep real keys inside local .env only,
# which is git-ignored. See SUPABASE_SECRETS_REMINDER below.
# =============================================================================

# --- NEW STA Core Supabase project (create under org iyngtxvchqpeayvsuvph) ---
SUPABASE_URL=https://<NEW_STA_CORE_REF>.supabase.co
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_PROJECT_REF=<NEW_STA_CORE_REF>

# --- Existing app projects (deploy-gated, read-only until approved) ---
MWAROKIN_PROJECT_REF=spnerrqumefbuuscumhw
MALI_PROJECT_REF=vfkqjapegrhdsrlmmiih
STA_ORG_ID=iyngtxvchqpeayvsuvph

# --- Mobile Money aggregator (PesaPal / M-PESA Daraja / Payfonte) ---
MOBILE_MONEY_API_URL=
MOBILE_MONEY_API_KEY=
MOBILE_MONEY_WEBHOOK_SECRET=

# --- OpenRouter (LLM triage + agentic features) ---
# TODO: ROTATE this key. The previous one lived in a plaintext Desktop file (STAOpenRouter APIs.txt).
# In OpenRouter dashboard: create a new key, paste here, then delete the plaintext file.
OPENROUTER_API_KEY=

# --- Banking (Setup task 4): Co-operative / I&M business account 01192643932500 ---
STA_BUSINESS_ACCOUNT=01192643932500
COOP_BANK_CONTINENTAL_API_KEY=
IM_BANK_API_KEY=

# --- Annual subscriptions (Setup task 5): provider API tokens ---
ANTHROPIC_API_KEY=
RESEND_API_KEY=
VERCEL_TOKEN=
NETLIFY_TOKEN=
GOOGLE_CLOUD_SERVICE_ACCOUNT_JSON=

# --- Web app (Next.js) ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# --- MALI Access Union (dedicated client — publishable key, gitignored apps/web/.env.local) ---
# NEXT_PUBLIC_MALI_SUPABASE_URL=https://vfkqjapegrhdsrlmmiih.supabase.co
# NEXT_PUBLIC_MALI_SUPABASE_ANON_KEY=<anon key in apps/web/.env.local>